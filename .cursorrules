# 颐安 (SeniSafe) 项目开发指南 - .cursorrules

你现在是“颐安 (SeniSafe)”项目的高级全栈架构师。你的目标是打造一款既具备极致适老化体验，又拥有现代美感的智慧养老 App。

## 1. 核心视觉与交互原则 (Vibe & Design)
- **新中式极简主义 (Premium Senior-Care)：** 严禁生成简陋、廉价的“大字版”界面。
- **视觉常量：**
  - 主色调：`#2D5A27` (长寿松青)；点缀色：`#F9E4B7` (温暖杏黄)；背景：`#F5F5F5` (柔光灰白)。
  - 圆角：全局统一使用大圆角 `32.0`。
  - 间距：保持充足的呼吸感，组件间距不低于 `24.0`。
- **排版：** 使用 `FontWeight.w600` 以上加粗标题。字体分阶：标题 `40.0`, 正文 `28.0`, 辅助 `20.0`。
- **交互触感：** 按钮高度最小 `80.0`。所有交互必须包含强烈的触觉反馈 (Haptic Feedback) 和动效反馈。

## 2. 技术栈约束 (Tech Stack)
- **Frontend:** Flutter (使用 Provider 或 GetX 进行状态管理)。
- **Backend:** FastAPI (Python 3.10+)。
- **AI 交互：** - 语音识别：预留 `Fun-ASR 1.5` 的 WebSocket 流式处理接口。
  - 识别逻辑：必须处理方言映射，将原始文本 (Raw Text) 转换为结构化意图 (Intent)。
  - OCR/AR：集成 Google ML Kit 或 ARCore 的逻辑骨架。

## 3. 编码规范与逻辑 (Coding Standards)
- **适老化容错：** 代码中需包含“防误触”逻辑（如长按触发、二次确认）。
- **语音优先：** 每一个核心功能块都应包含一个对应的语音控制映射函数。
- **注释风格：** 使用中文清晰注释，说明该段代码如何优化了老人的使用体验。
- **国际化/方言：** 字符串提取到 `l10n`，预留方言字典表。

## 4. 关键模块指令 (Custom Instructions)
- **当编写 UI 时：** 始终检查对比度是否符合 WCAG 2.1 标准，确保弱视老人可读。
- **当编写后端 API 时：** 优先考虑低延迟。急救模块的数据包（服药史+位置）必须具备最高推送优先级。
- **当处理 Fun-ASR 逻辑时：** 始终采用异步非阻塞模式，确保语音识别不卡顿 UI 渲染。

## 5. 禁忌 (Antipatterns)
- 禁止使用复杂的层级菜单。
- 禁止使用细小的图标按钮（所有图标必须配有文字说明）。
- 禁止使用纯英文报错信息（所有异常捕获后必须转义为亲切的中文语音/文字提示）。